{
  "name": "satyarth-games",
  "private": true,
  "version": "1.0.0",
  "description": "A portfolio of handcrafted browser games",
  "workspaces": [
    "packages/*",
    "AnimalRescue",
    "CozyKitchen",
    "MagicSort"
  ],
  "scripts": {
    "dev:animal": "npm run dev --workspace=animal-rescue",
    "dev:kitchen": "npm run dev --workspace=cozy-kitchen",
    "dev:magic": "npm run dev --workspace=magic-sort",
    "build:animal": "npm run build --workspace=animal-rescue",
    "build:kitchen": "npm run build --workspace=cozy-kitchen",
    "build:magic": "npm run build --workspace=magic-sort",
    "build:all": "npm run build --workspaces",
    "build": "mkdir -p dist && cp index.html styles.css dist/ && cp -r assets dist/ && cd AnimalRescue && npm run build && cp -r dist ../dist/AnimalRescue && cd ../CozyKitchen && npm run build && cp -r dist ../dist/CozyKitchen && cd ../MagicSort && npm run build && cp -r dist ../dist/MagicSort",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}
